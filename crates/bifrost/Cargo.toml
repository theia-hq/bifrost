[package]
publish = false  # git-only for now, not published to crates.io
name = "bifrost"
description = "Open a byte stream to a peer by its ed25519 key; the transport underneath is swappable."
version = "0.2.3"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bifrost-core.workspace = true
bifrost-transport.workspace = true
bifrost-wire.workspace = true

[lints]
workspace = true
