[package]
publish = false  # git-only for now, not published to crates.io
name = "bifrost-transport"
description = "The Transport, Session, and Discovery traits, and the security profile every backend declares."
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bifrost-core.workspace = true
tokio.workspace = true

[lints]
workspace = true
